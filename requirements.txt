# Core dubbing pipeline (dub.py also auto-installs these on first run).
faster-whisper
deep-translator
edge-tts
pydub
piper-tts
demucs
# Pin to the 1.x line: the voice-clone add-ons (coqui-tts/XTTS, OpenVoice and their
# ml_dtypes/transformers deps) ship binaries built against NumPy 1.x and crash on
# import under NumPy 2.x. Letting this float to 2.x silently disables XTTS, which
# drops English back to the flat edge-tts voice. See SETUP_VOICECLONE.md.
numpy>=1.26,<2
# Drag-and-drop GUI (dub_app.py)
tkinterdnd2
sv-ttk
# NOTE: ffmpeg is required but is NOT a pip package. Install it separately and put
# ffmpeg.exe + ffprobe.exe on PATH, or drop an "ffmpeg*/bin" folder next to dub_app.py.
